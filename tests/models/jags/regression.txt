model {
  # Likelihood
  for (i in 1:N) {
    y[i] ~ dnorm(mu[i], tau)
    mu[i] <- alpha + inprod(X[i,], beta[])
  }

  # Priors
  alpha ~ dnorm(0, 0.01)
  for (k in 1:K) {
    beta[k] ~ dnorm(0, 0.04)
  }
  tau ~ dgamma(0.01, 0.01)

  # Derived quantities
  sigma <- 1 / sqrt(tau)
}
