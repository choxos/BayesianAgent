model {
  # Likelihood
  for (j in 1:J) {
    y[j] ~ dnorm(theta[j], prec[j])
    prec[j] <- pow(sigma[j], -2)
  }

  # Group-level model
  for (j in 1:J) {
    theta[j] ~ dnorm(mu, tau.theta)
  }

  # Hyperpriors
  mu ~ dnorm(0, 0.0001)
  tau.theta <- pow(tau, -2)
  tau ~ dunif(0, 100)
}
